# 캡티브 포털 전용 DNS 서버
# 차단/격리 단말의 DNS 쿼리를 nftables가 이 포트로 리다이렉트한다.
# 모든 도메인 쿼리에 NAC Pi IP를 반환 → 브라우저가 캡티브 포털로 이동

port=5353
no-resolv
no-poll
bogus-priv

# 모든 도메인 → NAC_IP (환경변수로 주입, 기본값 192.168.0.39)
# 실제 IP는 docker-compose의 NAC_IP 환경변수로 설정
address=/#/192.168.0.39
