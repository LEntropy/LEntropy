{{- if .Values.apiGateway.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nac-platform.fullname" . }}-api-gateway
  labels:
    {{- include "nac-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-gateway
spec:
  replicas: {{ .Values.apiGateway.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-gateway
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: api-gateway
          image: "{{ .Values.apiGateway.image.repository }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.apiGateway.service.port }}
              name: http
          env:
            {{- include "nac-platform.commonEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.apiGateway.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nac-platform.fullname" . }}-api-gateway
  labels:
    {{- include "nac-platform.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.apiGateway.service.port }}
      targetPort: http
{{- if .Values.apiGateway.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "nac-platform.fullname" . }}-api-gateway
  labels:
    {{- include "nac-platform.labels" . | nindent 4 }}
spec:
  ingressClassName: {{ .Values.apiGateway.ingress.className }}
  rules:
    {{- range .Values.apiGateway.ingress.hosts }}
    - host: {{ .host }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "nac-platform.fullname" $ }}-api-gateway
                port:
                  name: http
          {{- end }}
    {{- end }}
  {{- if .Values.apiGateway.ingress.tls }}
  tls:
    {{- toYaml .Values.apiGateway.ingress.tls | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
